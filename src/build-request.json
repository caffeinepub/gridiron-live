{
  "kind": "build_request",
  "title": "Move scoreboard and flag announcements onto the video (on-camera), switch to manual score entry, and remove point button",
  "projectName": "VillanSpoon Broadcast",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-14",
      "text": "Replace the Viewer Watch page’s fixed bottom-of-screen scorebar with an on-camera (over-video) bottom scoreboard overlay rendered inside the video container, visible while watching.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "just the on camera one because its hard to see the on screen one"
        ]
      },
      "acceptanceCriteria": [
        "On the viewer watch page, the fixed/sticky BottomScorebar UI is no longer shown at the bottom of the screen.",
        "A scoreboard overlay is rendered on top of the video area and anchored to the bottom of the video frame.",
        "The overlay shows both teams’ selected icons and current scores and continues to update as the backend scoreboard changes."
      ]
    },
    {
      "id": "REQ-15",
      "text": "Add the text label \"VillanSpoon\" in the left corner of the on-camera scoreboard overlay on BOTH the broadcaster camera preview and the viewer stream view.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "also put VillanSpoon in the corner of the scoreboard",
          "for 1 exactly for 2 left and 3 both"
        ]
      },
      "acceptanceCriteria": [
        "The on-camera scoreboard overlay includes the exact text \"VillanSpoon\".",
        "The label is positioned in the left corner of the overlay (top-left or bottom-left within the overlay, but clearly in the left corner).",
        "The label appears in both the broadcaster camera preview overlay and the viewer on-video overlay."
      ]
    },
    {
      "id": "REQ-16",
      "text": "Change broadcaster score editing to manual score entry (no +/− buttons) while keeping live-updating custom scores persisted to the backend.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "manual",
          "get rid of the button keep the custom scores"
        ]
      },
      "acceptanceCriteria": [
        "Broadcaster can manually enter Team A and Team B scores via numeric input fields (or equivalent manual entry UI) instead of increment/decrement controls.",
        "Saving/updating scores uses the existing scoreboard update flow so viewers see the updated scores.",
        "Invalid values are prevented or handled safely (e.g., non-numeric rejected; negative scores not allowed)."
      ]
    },
    {
      "id": "REQ-17",
      "text": "Remove the \"Score\" / point button from the broadcaster game controls so the broadcaster cannot add a point/score event via that button.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "you stiil need to get rid of the point button",
          "get rid of the button keep the custom scores"
        ]
      },
      "acceptanceCriteria": [
        "The broadcaster UI no longer displays a \"Score\" (point) button in the game controls.",
        "No point/score event is emitted from the removed button path."
      ]
    },
    {
      "id": "REQ-18",
      "text": "Update the broadcaster \"Flag\" control to require selecting which team the flag applies to and entering a reason, then publish that flag announcement so it appears as an on-camera overlay over the video (visible to both broadcaster and viewers).",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "the flag button you slect and the reason why",
          "yes but put it on the scoreboard",
          "announce it om the camera not the screen",
          "2 yes"
        ]
      },
      "acceptanceCriteria": [
        "When the broadcaster triggers a flag, the UI collects (a) the team (Team A or Team B) and (b) a reason text before publishing.",
        "The published flag announcement is visible as an overlay on top of the video area (not only in the side feed / below the video) for viewers.",
        "The broadcaster camera preview also shows the same flag overlay on top of the video area.",
        "The overlay text includes both the selected team and the entered reason in English user-facing text.",
        "The flag announcement is persisted/published through the existing backend event mechanism so late-joining viewers can still see it in the event feed."
      ]
    },
    {
      "id": "REQ-19",
      "text": "Ensure the viewer’s latest-event presentation is on-camera (over-video) rather than a separate card below the video when an event arrives (especially flags).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "announce it om the camera not the screen"
        ]
      },
      "acceptanceCriteria": [
        "Latest event UI is rendered as an overlay within the viewer video container (absolute-positioned over video), not as a normal-flow element beneath it.",
        "Overlay remains temporary/auto-dismissed similar to the current latest event behavior."
      ]
    }
  ],
  "constraints": [
    "Backend must remain a single Motoko actor in backend/main.mo (no additional backend services).",
    "Do not edit files under frontend/src/components/ui or any other paths listed as immutable in SYSTEM_CONTEXT.",
    "Use English for any user-facing text."
  ],
  "nonGoals": [
    "Do not add third-party streaming providers or external databases.",
    "Do not add new authentication providers beyond Internet Identity.",
    "Do not redesign unrelated pages outside broadcaster/viewer watch flows beyond what is necessary to implement the on-camera overlays and control changes."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}